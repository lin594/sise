# 测试与验收

## 1. 自动化命令

在项目根目录执行：

```bash
npm ci
npm ci --prefix server
npm ci --prefix client
npm --prefix server test
npm run build
npm run e2e:responsive
npm run e2e
npm --prefix server audit --omit=dev --registry=https://registry.npmjs.org
npm --prefix client audit --omit=dev --registry=https://registry.npmjs.org
```

- 服务端测试同时运行旧版规则回归、Node 测试套件和机器人完整牌局压力测试。
- Playwright 启动的隔离测试服务提高房间创建限额，并缩短无浏览器连接房间的回收时间，避免串行用例共享来源地址或遗留机器人牌局相互干扰；生产和 Docker 默认值保持不变。结算布局回归在真人完成声明后使用仅测试环境、仅房主可用的确定性胡牌场景，避免依赖随机牌局何时自然结束。
- `npm run build` 构建服务端、客户端，并同步检查 TypeScript 与生成版客户端文件。
- `npm run e2e:responsive` 会先重新生产构建，再让同一份确定性结算依次切换全部代表视口；Chromium 与 WebKit 都执行这条移动关键流，避免旧 `dist` 造成假失败或假通过。首次准备测试机时需执行一次 `npx playwright install webkit`。
- `npm run e2e` 同样先重新构建；全部浏览器流程由 Chromium 执行，移动响应式发布门禁另由 WebKit 再执行一次。
- Playwright 默认启动本地前后端。若机器只有系统 Chrome，可以使用：

```bash
PLAYWRIGHT_CHANNEL=chrome npm run e2e
```

- 两条生产依赖审计命令显式使用 npm 官方 registry，因为部分镜像站没有实现审计接口；当前基线要求服务端和客户端均为 0 个已知生产依赖漏洞。

提交前还应运行：

```bash
git diff --check
```

## 2. 服务端回归矩阵

### 房间与身份

| 场景 | 预期 |
|---|---|
| 单人练习开局 | 一名真人可以开局，余下三席由机器人补齐 |
| 快速配桌 | 首人建立 12 秒权威截止时间，普通加入与刷新不延后；四名真人到齐加速到约 1 秒，截止或首位玩家立即开始时只用标准机器人补空座 |
| 快速桌容量与断线 | 座位固定自动分配；保留真人离线时暂停开局且保留原截止时间，四个保留座已满时关闭 `matchOpen`，座位释放后才重新开放 |
| 快速桌权限 | 不允许选座、调整计分、管理机器人、整桌下一局或整桌返厅；结算重配只移动点击者 |
| 本机临时档案 | 只接受独立 `gp_` 高熵凭证；HTTP 只用 Bearer 且禁止缓存。权威结算按房间/局数幂等累计真人局数、胡牌数和总分，机器人忽略，临时托管仍归真人；Redis 失败不影响牌局，故障期间的最后昵称和唯一结算进入内存补写队列，冷却期不反复探测，恢复后无需重启即可幂等补写；已写入但响应丢失的结算不得重复累计 |
| 机器人昵称 | 从友好昵称池随机选择；同桌机器人之间及与真人之间均不重名，刷新和本局结算期间保持稳定 |
| 机器人身份 | 牌桌、小屏操作提示和结算均有“电脑/机器人”标识，不依赖昵称前缀；真人临时托管仍保留真人昵称 |
| 好友房创建与邀请 | 邀请链接携带 roomId，受邀者可以原子领取空座 |
| 练习房入口隔离 | 并发调用 `/room-id` 必须返回不同房间；每个练习房只允许首位新真人，第二个新 token 被拒绝，而原 token 仍可重连 |
| 同名与重连 | 同房真人同名自动追加可读后缀；断线、刷新或故意提交不同昵称后仍恢复首次入座的稳定名字 |
| 房主管理机器人 | 只有房主可添加、移除机器人；界面用轻松/标准/高手三个大按钮发送 20/50/85，服务端仍安全接受并限制旧客户端的 0–100 强度 |
| 一键补齐电脑 | 好友房房主在固定操作区看到准确的“补齐 N 位电脑”；一次补满所有空座且保留既有成员，机器人昵称唯一、默认标准难度，只广播一次，不自动开局；非房主、满桌和非好友房请求不修改状态 |
| 好友房准备 | 非房主真人入座后显示大字准备按钮和“未准备”；准备后全桌同步“已准备”，所有真人在线且准备前房主开始按钮保持禁用。取消准备、换座、首局计分方式变化和返厅会重新阻止开局；短暂断线保留选择但离线本身仍阻止开局；房主、机器人及牌局中请求不能伪造准备 |
| 好友房准备回执 | 568×320 暂缓 `set_lobby_ready` 时，“我准备好了”立即变为禁用的“正在准备…”并显示请求已发送；同一帧双击只积压一条 `true`，权威成功后普通玩家按钮与房主座位状态同步。取消准备同样只积压一条 `false`；8 秒无确认后恢复原按钮并可重试，不提前乐观改写座位 |
| 未入座访客同步 | 好友邀请访客未选座时能查看大厅但不请求 `/private-state`，控制台不出现未处理 `lobby_presence`；领取座位后立即获得私有状态并恢复周期同步 |
| 好友房选座回执 | 568×320 访客暂缓 `claim_seat` 后，目标空位显示“正在入座…”和准确 A/B/C/D 确认提示，其余空位按钮锁定；同一帧依次点击两个空位只积压第一条消息，释放后只进入首次选择。已入座换座显示“正在换座…”，8 秒无确认后恢复按钮并可再次提交，成功后准备与私有状态同步正常 |
| 等待大厅重连 | 保留期内同 token 恢复原座，过期后释放 |
| 创建后未加入 | HTTP 创建即启动等待房回收计时；宽限内可以完成首次加入，始终无人加入则断开并释放房间 |
| 对局中断线 | 默认 5 秒内保持离线真人且机器人不动作；到期后原座进入托管但昵称不变，标准屏显示“暂由机器人”，极小屏显示“托管中” |
| 断线操作反馈 | 保留牌桌与手牌；顶部明确显示自动恢复，右侧操作坞以“操作已暂停”状态替换所有动作按钮，恢复前没有可点击牌局操作 |
| 对局中重连 | 同 token 恢复原座和真人控制，另一浏览器观察到同一昵称恢复为“真人在线” |
| 服务重建恢复 | 活动房完整快照保存公开状态、四家私有手牌、牌堆、待响应选择和 token 映射；服务启动在监听前恢复原 roomId。旧 session/计时器不复用，真人获得新重连宽限，原 token 回到原座后可继续当前决定；只替换 server 容器且 Web 网关保持原容器时，Docker DNS 地址变化也能自动刷新；正常解散的房间不得复活 |
| 同座位多窗口 | 新窗口用同 token 恢复后，服务端通知并关闭旧会话；旧页进入终止状态且不再抢座，新页持续保持同一 seatId |
| 房主断线与退出 | 刷新或意外断线在宽限期内保留房主，及时恢复后权限不变；宽限到期才转给座位序号最小的在线真人。确认主动退出则立即释放/托管并转交，不等待断线宽限 |
| 房主解散好友桌 | 仅好友房等待阶段的房主看到并可操作；二次确认可取消并恢复焦点，确认后所有连接停止恢复、清除房号/token 并回模式选择。普通成员、单人练习和进行中的牌局不能解散整桌 |
| 好友房累计计分 | 默认每局单算；仅房主可在首局前切换。本桌累计时逐局按四个固定座位相加且保持零和，下一局和整桌返厅均保留；首局结算后选择锁定，解散才结束榜单 |
| 私有手牌 | 每个真人只收到自己的牌和动作候选 |
| 私有状态接口 | 新客户端用 Bearer token，URL 不含凭证，响应为 `no-store`；旧查询参数只作滚动兼容 |
| 房间凭证 | 新凭证匹配 `pt_` 加 48 位十六进制字符、批量生成不重复；旧格式仍可恢复已有座位 |
| 浏览器来源 | 生产模式允许配置来源访问 HTTP、匹配与 WebSocket；未知来源被拒绝，域名后缀伪装不能通过 |
| 正式传输地址 | HTTPS 页面默认得到 HTTPS/WSS；只显式配置 HTTPS 或 WSS 任一基础地址时另一项使用相同主机、端口和路径推导；HTTP 试玩仍得到 HTTP/WS，IPv6 本机地址保持合法 |
| 调试入口 | 生产环境无条件禁用 `debug_setup`；非生产环境也必须显式开启且只有房主可用，普通玩家不能改写牌局 |
| 管理入口 | 生产模式默认访问不到 `/colyseus`；只有显式启用后才开放诊断页 |
| HTTP 限流 | 创建房间、入口查询和私有状态恢复各有独立预算；超限返回 429、`Retry-After` 和 `rate_limited`，窗口结束后恢复 |
| HTTP 错误说明 | 浏览器可读取限流等待时间并显示中文秒数；5xx 响应中的内部错误详情不会进入玩家界面 |

### 规则主循环

| 场景 | 预期 |
|---|---|
| 集体响应 | 胡、开、碰、过按服务端顺序与优先级裁决 |
| 上家牌本地响应 | 可吃时提供候选；不吃显示“抓”，协议提交 pass |
| 自摸普通牌本地响应 | 可吃或过给下家，过出的牌进入本人弃牌记录 |
| 最高分胡牌拆解 | 同一手牌有多个合法拆解时选择总分较高者 |
| 胡牌响应组成三张 | 按碰展示计分，不按暗坎 |
| 互付文案 | 显示“阿福（机器人） 付 金条开：+18分”，付款人、`付` 和计分项之间各有一个空格 |

### 特殊牌关键回归

必须使用完整房间流程覆盖四色将、五种金条、不同机器人强度和超时分支：

1. 构造当前玩家手里有绿士、绿象，随后摸到绿将。
2. 前端在集体阶段预选“绿将＋绿士＋绿象”，其他响应者全部过。
3. 服务端进入 `local_draw`，接受预选候选。
4. 断言绿士、绿象从手牌中同时消失，明示区只有一个三张牌组且恰为绿将、绿士、绿象。
5. 断言没有额外的单绿将牌组，并且完成组牌后才进入弃牌确认阶段。

还要覆盖：

- 玩家主动选择单张吃牌时，明示区新增一个一张牌的将/金条牌组，不写入独立特殊牌区。
- 多个候选存在时等待选择；超时默认按单张吃牌处理。
- 任意强度机器人都不得把特殊牌弃出或传给下家；胡牌与合法开仍保持原优先级。
- 候选过期、手牌 ID 缺失或目标牌变化时，执行原子失败：手牌、牌池、明示区与流程阶段均不发生部分修改。
- 吃、碰、开共享扣牌路径的普通牌回归仍通过。

## 3. 浏览器回归矩阵

Playwright 的有效横屏矩阵为 568×320、667×375、740×360、812×375、844×390、852×393、896×414、915×412、926×428；物理竖持自动旋转矩阵为 320×568、375×667、390×844、393×852、412×915、428×926，另保留 1024×768 平板与 1280×720 桌面基线。

| 场景 | 验收点 |
|---|---|
| 页面缩放 | viewport 不含 `maximum-scale` 或 `user-scalable=no`，系统双指缩放不被页面禁止 |
| 旧浏览器视口 | `html/body/#app` 在 `100dvh` 前保留 `100vh`；568×320 根布局的物理像素变量为 `568px × 320px`，切到 320×568 后同步为 `320px × 568px`，有效画布仍为 `568×320`，旋转根节点和全部弹层保持在物理屏幕内 |
| 首次入口 | 568×320 昵称页明确说明“不用注册、不用密码、这台设备会记住昵称”，只有一个顶栏规则入口，卡内只有“下一步：选择玩法 / 换个名字”，入口小标题不小于 13px、输入字不小于 18px、按钮字不小于 16px，顶部“查看规则 / 修改昵称”不小于 14px；玩法页同屏显示单人练习、快速配桌、好友同桌三种可用模式，默认模式获得焦点并有 `aria-pressed`。顶栏显示昵称，“修改昵称”返回后保留输入并聚焦；320×568 自动旋转和 1280×720 同样完整可达 |
| 玩法启动慢网反馈 | 延迟建房或配桌请求时，568×320 的创建按钮立即禁用并显示所选玩法对应的中文进度，三张模式卡同时锁定，状态提示“不用重复点击”，连续触发只产生一次请求；500/限流/网络失败不显示内部详情，并恢复原玩法、昵称与可重试按钮。快速配桌使用“正在寻找牌友”进度页，取消后回到仍选中快速配桌的玩法页，迟到连接不能把页面重新带回房间 |
| 房间内开局回执 | 暂停 `start_game` 发送时，练习自动开局、好友房开始和快速配桌补位都立即锁定按钮并显示对应中文进度与“请求已发送”；好友房同一帧连续点击只积压一条消息。8 秒没有权威阶段变化后恢复原按钮并提示重试，再次提交可正常进入声明；真实快速响应仍直接切换牌局，不闪回普通按钮 |
| 本机档案摘要与详情 | 模式大厅显示零局或“已玩 N 局 · 胡 N 局”，摘要明确可查看，在 568×320 与自动旋转画布中至少 40px 高、可见文字至少 14px，且不挤出三种玩法和开始按钮；详情显示昵称、局数、胡牌数、胡牌率和累计总分，说明清除浏览器数据后无法找回。零局胡牌率为“—”，结算后按服务端结果刷新，换房和刷新仍保留；Tab 不离开详情，Escape/遮罩/关闭后焦点返回摘要。档案 token 不得出现在 URL、邀请链接、公开 Schema、页面文字或普通日志 |
| 浏览器存储受限 | 页面脚本运行前令 Web Storage 的读、写、删全部抛错；568×320 首页仍显示非空可编辑昵称、字号不小于 14px 的诚实限制说明且无溢出。可进入三模式大厅和单人练习声明，不显示本机档案摘要、不承诺设备记忆或刷新恢复，全程没有 `pageerror` |
| 好友邀请重复提交 | 延迟加入请求并在同一帧连续触发两次回车；只发起一次匹配，进度页明确写“加入好友房/正在进入朋友的牌桌/取消加入”，不出现恢复手牌文案，成功后没有伪造的失败提示。无效房间则回到原邀请昵称页，保留昵称、roomId 和可用的重试按钮；连接中取消会回普通昵称页，清除地址房号及尚未入座的房间 token，迟到连接不能把页面带回房间 |
| 快速配桌等待 | 两个独立浏览器进入同一 roomId、自动占不同固定座位；页面只显示真人数量、权威倒计时、个人离开和首位玩家“电脑补位，立即开始”，不显示邀请、选座、准备、计分或机器人管理。即使手机时钟与服务器相差一分钟，首屏也不得超过服务端配置的等待秒数；刷新后秒数继续减少且 URL 不出现 roomId/token；倒计时结束自动补位开局 |
| 快速桌个人重配 | 四家结算到齐后每名真人都看到“再来一局（重新配桌）”，不显示房主下一局/返厅；点击者进入新房并获得新 token，未点击者仍留在原结算页 |
| 练习结算导航 | 只显示“再练一局”和“返回玩法选择”；返回后停止旧房重连、清除凭证，并同时看到单人练习、快速配桌、好友同桌三个入口 |
| 浏览器与主屏入口 | 首页只引用一份 Manifest，并提供主题色、SVG/ICO favicon、Apple Touch Icon、旧 iOS standalone 名称/状态栏标记和 192/512 PNG；Manifest 名称、根入口、语言、颜色和 standalone 模式准确，PNG 尺寸与声明一致；Docker 中 Manifest/SVG/PNG 类型正确，缺失图标返回 404 而非首页 HTML；不注册离线 Service Worker |
| 顶部控制栏 | 声明、对局、结算均常驻；只含四色牌、记录、设置、托管、退出，控制文字在老旧小屏不小于 14px，且不与座位或弹层重叠 |
| 断线恢复状态 | 667×375、568×320 与 320×568 自动旋转画布中，断网、重连和恢复成功的主文字不小于 14px，完整位于顶栏并避开招牌与右侧控制；空间不足只隐藏辅助说明。可手动重试时按钮至少 36px 高、14px 字，连接状态机、牌桌保留和自动恢复行为不变 |
| 主动托管 | 在线真人开启前二次确认，默认聚焦“暂不开启”且背景不能抢走焦点；开启后保留昵称、真人及房主身份，顶部持续高亮“取消托管”，人工牌局动作禁用且机器人继续推进；取消一次点击立即生效。定庄揭晓期间切换不取消动画或提前显示声明；刷新重连保留选择，返回大厅清除；配置机器人不能调用该消息 |
| 开局声明 | 系统推荐完成后主按钮明确显示“开始游戏”和亮鱼/暗坎结果并获得焦点；568×320 显示“上下滑调整 · 手牌可前后翻 · 练习不限时”，练习模式、手牌总数、声明结果、选项说明和数量单位均不小于 13px，纯装饰步骤编号可为 12px；同步中按钮禁用且焦点位于弹层，Tab/Shift+Tab 不得逃到背景 |
| 声明断网恢复 | 断网后声明面板和已选亮鱼/暗坎原样保留，选项锁定且主按钮显示“等待网络恢复”；恢复后原选择不变、错误提示消失并可重新提交。发送失败或确认超时不能永久停在“提交中” |
| 声明手牌翻看 | 568×320 的大字/自适应预览不小于 40×44px、主字不小于 22px，长牌不小于 28×52px、主字不小于 16px；溢出时显示至少 44×36px、文字至少 13px 的“前翻 / 后翻”按钮，实际可见牌号范围也不小于 13px；可到达末尾并返回开头，范围同步更新且不改变牌序、亮鱼、暗坎或提交状态 |
| 最近操作 | 默认收起；打开后最新在前，使用昵称、具体牌名和完整中文动作且不出现内部座位/动作码；明确提示查看期间计时继续；667×375 内可完整滚动，关闭按钮至少 40px，记录正文不小于 14px；320×568 自动旋转时面板完整位于物理屏幕内；Escape 后焦点回到入口，新决策到来时自动收起；点击牌桌区域只关闭遮罩，不得误选手牌或触发动作 |
| 中央牌桌 | 左牌堆、右待响应牌的组合整体居中；牌堆在待响应牌出现前后位置固定 |
| 关键轮次信号 | 568×320 与 320×568 自动旋转画布中的“当前回合”“待响”、四方向动作反馈、庄家“庄”、本人“你”、本人非零牌组/暗坎摘要和记录数量实际字号均不小于 13px；操作坞的当前操作、倒计时/“练习不限时”、具体说明和“轮到你时会提醒”同样不小于 13px；牌堆单位“张”不小于 10px 且字形不被裁切，放大后仍不遮牌、不越出座位且页面无溢出 |
| 紧凑牌桌边界 | 568×320 下牌桌自身 `scrollHeight` 不超过 `clientHeight`，上方、左右、中央和本人明示牌区均在牌桌边界内；同一局切换到 320×568 自动旋转后，牌桌及底部手牌操作行的逻辑高度与横持相差不超过 1px，牌字和按钮不缩小 |
| 牌堆造型 | 八张细长、两头圆、红色牌背重叠，余量数字准确且可访问 |
| 庄家信息与揭晓 | 揭晓前不提前显示庄家边框或常驻牌面；依次呈现红色细长圆头牌背、真实定庄牌与“某某坐庄”，读屏获得同序信息，真实牌面停留足够辨认且整局只创建一轮 81 张发牌飞牌；减少动态偏好下无翻转/飞行。落位后只在唯一庄家名字旁出现庄标和定庄牌，不显示抽牌者或重复庄家姓名；本人坐庄时，庄标和牌面在 568×320、320×568 自动旋转、667×375 和 1280×720 中均完整位于左下角本人信息卡内 |
| 对手手牌数 | 三位对手均显示准确“n张”，摸牌、组牌和弃牌后及时更新；568×320 与 320×568 自动旋转画布中的余牌数字和“电脑”标识均不小于 13px |
| 显示模式 | 我的牌与桌面牌分别切换大字/自适应/长牌，所有对应牌区即时变化，刷新后保留 |
| 长牌与明示组 | 667×375 的明示长牌主字至少 11px、牌面至少约 19×37px；1280×720 主字至少 12px、牌面至少约 21×44px。周边玩家同组牌在大字、自适应和长牌模式下都轻微错位折叠但主字不互相覆盖；本人牌组保留宽松查看。待响应标识使用边框而非压住文字的星形覆盖 |
| 座位顺序与方向 | 好友按乱序入座或换座后，牌桌仍按 A→B→C→D 的 `seatIndex` 映射显示；左右玩家及相关动画可镜像，自己、对家和服务端行动顺序不变 |
| 设置与回合 | 声明、响应和出牌时均可打开；面板不透底、关闭按钮不小于 42px，主要选项不小于 14px、辅助说明不小于 13px；320×568 自动旋转时按568×320有效画布限高且完整位于物理屏幕内，内部可滚到常亮和规则入口，尚有下方内容时显示“下滑查看更多设置”且到达底部后消失。本人决策时面板固定显示“轮到你操作”和好友局剩余时间或“练习不限时”，并提供返回操作按钮；计时继续、选中牌不丢失，关闭后焦点回到已选牌、出牌确认或首个合法动作 |
| 浏览器返回保护 | 好友房等待、声明、对局、结算和恢复阶段按系统返回不离开页面；若设置/记录/规则/候选层已打开则只关闭该层，再次返回才出现对应离房确认。取消后连接与手牌不变；确认或页面内主动退出后进入正确大厅、停止重连、清除房间凭证与查询参数，当前历史记录不再含保护标记；刷新仍可恢复原座 |
| 规则速查 | 568×320 与 320×568 自动旋转画布中使用单列纵向阅读且无横向溢出，正文不小于 15px、关键标签不小于 14px；内容可纵向滚动，标题和不小于 42px 的关闭按钮保持可见，Escape 后焦点返回原入口 |
| 规则与回合 | 规则速查具有模态语义、Escape 与焦点约束；本人决策期间仍可主动打开，明确提示计时继续并显示剩余时间或“练习不限时”；关闭后保留选择并聚焦可选手牌、确认出牌或合法动作 |
| 座位摘要 | 可见界面不重复本人手牌数，不显示牌组/暗坎零值；每个座位的读屏摘要仍包含余牌、牌组、暗坎、连接身份与关键回合状态。667×375、568×320 下左右姓名独占首行且至少获得 40px 可见宽度，徽章位于下一行，余牌、“电脑”文字及本人非零牌组/暗坎摘要不小于 13px，座位无水平溢出；本人昵称不再拼接“（你）”，独立“你”徽章可见且常见昵称完整显示 |
| 流水信息 | 空流水不显示边框或占位字；有牌时仅显示“出牌者 → 接牌者”，小屏标题字号不小于 13px，且保留完整无障碍说明。以非座位顺序加入的好友房中，四条关系仍按 `seatIndex` 正确；重复同状态同步时 MutationObserver 不得观察到流水容器或既有牌节点被移除重建，切换方向只镜像归属 |
| 空牌组区域 | 本人和对手没有公开牌组时不显示空边框或“暂无牌组”；首个牌组出现后正常展示，中央牌堆位置保持不变 |
| 紧凑手牌 | 667×375 下牌宽约 40–44px、间距约 2px、字形约 22px 以上，首屏约 9–10 张并可横滑 |
| 老旧小屏 | 568×320 下牌面不小于 40×52px、字形不小于 22px、首屏至少完整显示 8 张；本人手牌计数与可见牌号范围不小于 13px，本人信息、手牌和操作坞互不交叠，全部操作按钮至少 40px |
| 手牌翻看 | 手牌溢出时显示“‹ 前翻 / 后翻 ›”文字按钮，小屏至少 44×36px、文字至少 13px；按钮之间的实际可见牌号范围同样不小于 13px，并具有完整中文无障碍名称，首屏从第 1 张开始、后翻同步增加、末尾结束序号等于总张数、前翻可回到第 1 张；期间不改变手牌、选择或触发出牌，触摸横滑仍正常 |
| 手牌操作就绪 | 发牌仍显示“当前张数/总张数”或公开 `handCount` 与私有手牌长度不一致时，声明确认、选牌和动作均禁用并显示整理/同步提示；完整手牌到齐后重算声明建议并开放操作，选牌到确认之间牌组不得自行增减 |
| 牌面可访问性 | 每张牌暴露“颜色＋牌名”，金条读作“金条＋爵位”，待响应状态有文字语义；所有正面牌显示正确的“黄 / 红 / 绿 / 白 / 金”文字印且不遮主字，红底黑字对比度至少 4.5:1，键盘焦点清晰 |
| 确认出牌 | 单击、重复点击和双击只选牌；选中后提示与操作坞按钮显示同一完整牌名（如“已选红相”“打出红相”），牌名不换行、不越界；只有该按钮提交，普通慢响应期间按钮保持提交中，失败后恢复选择，选择失效后不残留旧牌名；本人出牌的本地预演与服务端确认合计只出现一次飞牌动画 |
| 权威操作回执 | 吃、碰、开、胡、抓/过和出牌点击后立即显示“正在确认”并锁定同一 `decisionKey`；服务端接受后显示一次“已收到”，文字收起后仍不能重复提交，进入新决策才解锁。过期、候选失效、非法弃牌与断网发送失败显示中文原因并可重试，旧回执不得遮挡或锁住下一次操作 |
| 多候选动作 | 吃/碰/开有多种组合时聚焦第一项并暴露模态名称与说明；Tab 焦点留在候选层，Escape/取消/遮罩只关闭选择并回到原动作按钮 |
| 手牌状态 | 等待、候选和断网阶段全部手牌保持完整对比度；弃牌阶段仅将牌/金条出现“留”标记和“规则保护”读屏说明，且仍清晰可辨 |
| 回合提醒 | 本人可操作时操作坞显示“该你操作了”和剩余秒数；667×375、568×320 下说明文字至少 13px 且不越出操作坞；最后 5 秒高对比警示且减少动态偏好有效 |
| 非本人回合 | 操作坞位置不变，但没有任何灰色动作按钮；只显示当前玩家“正在操作”和“轮到你时会提醒”的非按钮等待卡。568×320 下主字不小于 15px、辅助字不小于 12px，断线时由暂停状态取代 |
| 集体响应预选 | 后位真人在前位尚未响应时立即看见并可点击自己的胡、开、碰、过或本家预吃/抓；操作坞写“现在可以先选”，不显示前位倒计时。提交不清除前位计时、不抢跑游标，最终动作优先级与座次规则不变 |
| 选牌状态 | 选中牌同时有位移、描边和勾选标记，读屏名称以“已选中”结尾；双击仍只改变选择，不直接出牌 |
| 声音与震动 | 新操作窗口只提醒一次；默认响铃加震动，可切为仅响铃或关闭；不支持 Web Audio/振动时不影响操作，标签页标题仍提示“轮到你了” |
| 语音提示 | 默认关闭且与响铃/震动互不覆盖；开启后每个新决策只播报一次实际动作，弃牌明确说“先选牌，再点出牌”，普通状态刷新不重复，刷新页面后偏好保留；Speech Synthesis 不可用时入口禁用并显示原因，牌局仍可操作 |
| 减少动态效果 | 游戏内开关默认关闭、即时生效并刷新保留；开启后飞牌/定庄飞行隐藏、循环动画与过渡近乎为零、手牌翻页使用即时滚动，但信息停留和服务端节奏不变。系统 `prefers-reduced-motion` 单独开启时同样静态显示，且不擅自改写本地开关 |
| 屏幕常亮 | 默认在声明/对局前台申请 Wake Lock；关闭设置、切后台、结算或退出时释放；API 不存在或当前页面不是安全上下文时开关明确显示“不可用”，但保留偏好且牌局照常可用 |
| 决策时间 | 单人练习的在线真人声明、响应和出牌明确显示不限时且不出现加时按钮；机器人或断线托管仍会推进。好友房默认响应/出牌 30 秒、声明 45 秒，每名真人每个决策窗口可加时一次 20 秒；机器人、断线玩家、重复请求、预选玩家和旧窗口迟到请求均不能加时。在线真人只有“过”可用时仍显示与普通待响一致的固定 3 秒窗口，提前点“过”只锁定选择而不提前推进，公开动作仍显示普通“过”；后位真人已私下预选时，到达该座位也至少保留 3 秒，不以瞬时跳过暴露预选或牌型。单人练习、机器人和托管座位不增加等待。吃、碰、开、胡、过与出牌也携带当前决策指纹，旧窗口的迟到提交不会作用到下一操作。生产默认机器人可见动作 450–850ms、集体待响 80–180ms、无人响应过渡 250ms |
| 特殊牌操作 | 无论单张还是组合候选，按钮、提示、反馈和记录都统一使用“吃”；结果进入普通明示区，不突出为独立特殊动作 |
| 结算到达顺序 | `ended` 已到但 `round_result` 尚未完整时显示整理中并锁定整桌操作；四家结果到齐后才允许下一局或返回大厅 |
| 权威版本仲裁 | Schema 补丁和完整快照携带相同递增 revision；先送新快照再送旧 Schema 时阶段、玩家排序和牌区不回退。同 revision 只补私有手牌/动作/计时，可见内容相同的新 revision 不替换公开 Vue 对象 |
| 动画单次呈现 | 每个房间和局数只触发一轮发牌；发牌阶段可见手牌数始终小于该局权威完整数量，只有 `DECLARING` 权威状态到达后才完整揭示。首局、下一局和减少动态效果路径均验证；新手牌不在旧结算页出现，慢设备按绝对时间跳帧。相同动作经重复、同版本或迟到快照到达时，出牌和吃碰开飞牌合计只播放一次 |
| 好友房下一局确认 | 好友桌仍有其他真人时，房主点击下一局先看到“所有牌友会离开结算”的确认，默认聚焦继续查看；Tab 不逃出，Escape/遮罩/取消回到原按钮且其他玩家保持结算。确认后全桌进入下一局；练习房和纯机器人好友桌不弹确认 |
| 小屏结算 | 全部有效高度不超过 500px 的代表视口中，本人摘要排第一且四家长明细默认折叠；固定结算头不超过 64px，中间明细区至少占结算面板高度 40%，320–428px 有效高度的折叠首屏至少完整显示前三家总分行，底部操作持续可见且不与明细相交。展开后牌宽不小于 32px、牌面字不小于 16px、摘要和明细文字不小于 12px，滚动到手牌或分数时当前玩家摘要仍留在可视区；只有中间明细区滚动，顶栏、本人输赢和房主操作始终可见且不覆盖内容 |
| 累计结算 | 好友房累计模式每家同时显示本局与累计分；连续两局后累计值等于两局之和，返厅显示四席榜单和已完成局数，计分按钮保持锁定 |
| 个人退出 | 打开时默认聚焦“继续游戏”，Tab 和任何异步/程序化焦点变化都留在弹层内，新操作窗口到来时也不能把焦点抢回背景牌桌；Escape/遮罩/取消均回到退出按钮；确认后停止重连、清凭证并回模式大厅，进行中空座由机器人托管。320×568 竖持自动旋转时，确认层必须挂在有效游戏画布内并完整位于物理视口，方向与牌桌一致 |
| 退出与迟到响应 | 退出前已返回但被网络延迟的旧 `/private-state` 响应到达后仍停留模式大厅，不恢复旧 seatId、手牌或恢复页 |
| 出牌与迟到响应 | 出牌前捕获的旧 `/private-state` 响应延迟到出牌成功后交付；已打出的牌不能重新出现，哪怕随后又被 WebSocket 消息纠正 |
| 好友邀请 | 邀请入口明确写“加入好友房”并说明选座；房主、未入座访客、已入座访客各自看到准确下一步；667×375 首屏可见至少 80px 高的第一排座位；始终显示“复制邀请链接”，即使系统分享可用也只复制纯 URL；另有“邀请牌友”只传包含完整 URL 的 text，不传独立 url、title 或 files；实际复制地址在独立会话可进入同房选座；两种入口都不含身份凭据，取消分享不复制、不报错，其他失败自动降级；忙碌时禁止重复触发并准确恢复焦点；三个邀请按钮在小屏完整可达且不挤掉首排座位；HTTPS/localhost 与普通 HTTP 局域网地址均可复制；两级自动复制均失败时改用站内大字弹层，链接自动全选、焦点不逃出、关闭后返回原按钮且绝不调用系统 `prompt`；“出示二维码”在本机生成无凭据邀请，568×320、320×568 自动旋转、667×375 和桌面内均保持正方形且完整可达，焦点不逃出并在关闭后返回入口；Canvas 生成失败时自动全选同一邀请链接 |
| 好友房等待与离开 | 标题、滚动座位和固定下一步互不覆盖；568×320 首排座位可见高度至少 80px，规则、离开、复制与开始按钮至少 42px 高。离开确认默认聚焦“继续等待”，Tab 不逃出，Escape/遮罩/取消回到触发按钮；确认后清除房号、房间 token 和 URL `roomId` 且不自动重连。320×568 自动旋转画布中的弹层仍完整位于物理视口内 |
| 机器人难度 | 好友房不显示精细滑杆；轻松/标准/高手三按钮在 667×375 内不越出座位卡，每个至少 48×42px，当前档同时有文字、边框和 `aria-pressed` 状态 |
| 结算整桌返回 | 仅房主可见；点击后先显示二次确认且默认聚焦“继续看结算”，Tab 焦点不逃出，Escape 取消并回到原按钮，取消时所有玩家仍停留结算页 |
| 结算整桌操作回执 | 暂停 `next_round` 或 `return_lobby` 时，568×320 的下一局与返厅按钮一起锁定，只在对应按钮显示准确进度并提示“整桌请求已发送”；同一帧双击只积压一条消息，两种命令不能并发。8 秒未确认后恢复并可重试，释放消息后分别进入声明或房间大厅 |
| 网络中断 | 离线画面保留牌桌与手牌并冻结动作；联网后只建立一个替代 WebSocket，取回原座且凭证不变，再以服务端最新状态覆盖，允许托管期间手牌发生合法变化；恢复提示在 568×320 顶栏内完整显示“请核对手牌”、字号不小于 14px、至少保留 6 秒且不遮挡控制；服务暂不可达时“立即重试”至少 36px 高、14px 字 |
| 页面刷新 | 有效凭证自动回到原房间和原座，不出现昵称输入；昵称、roomId 与 token 不变 |
| 失效历史房间 | 进入 `closed` 并明确显示已停止自动恢复；等待超过一个退避周期也不新增匹配请求，返回首页后清除房间凭证 |
| 终止性关闭 | 4100–4106 的座位/房间业务关闭和匹配层 522/524 不自动重试；普通断网与服务短暂不可用仍持续恢复 |

每轮视觉调整至少保存定庄完成、普通等待、待响应、选牌待确认、设置展开及两种座位方向截图，检查全局溢出、可读性与控件交叠。响应式发布门禁另外固定保存 568×320、844×390、915×412、390×844 自动旋转及 1280×720 的确定性结算截图，人工核对牌型阅读顺序。

## 4. 人工试玩清单

- 使用真实触屏完成：加入、声明、吃/碰/开/过、选牌确认、设置、退出和结算。
- 在 568×320 老旧小屏、667×375 iPhone SE、844×390、915×412 等现代全面屏尺寸重点检查老年玩家是否能辨认牌字、玩家名、余牌数和动作文字；把同一局切到对应竖持自动旋转尺寸，确认本人明示牌区没有伸出牌桌或被裁掉。
- 特殊牌完整走一次“单张吃牌”和一次“将士象”组合，核对手牌与明示区，而不只观察按钮是否出现。
- 故意错过一次动画后打开“记录”，确认能通过昵称和中文动作辨认上一位玩家做了什么；再制造超时和断线托管，确认系统处理也能回看。
- 刷新页面确认重连、手牌恢复与显示偏好持久化；在好友房以房主身份刷新一次，确认恢复后仍是房主。
- 完成一局后返回玩法大厅，核对本机档案的局数和胡牌数；刷新页面确认不重复累计，再用另一浏览器确认不会无意共享档案。
- 在单人练习结算分别验证“再练一局”和“返回玩法选择”：下一局看不到提前亮出的整手牌，也不会在暗坎声明后重发；返回后能改选快速配桌或好友同桌，浏览器不再自动连回旧练习房。
- 在本人选好弃牌但尚未确认时打开设置和规则，确认好友局倒计时继续、练习局显示不限时，关闭后仍选中原牌并聚焦回确认操作。
- 人为重复发送同 revision、迟到旧 revision 和同一动作快照，确认牌桌不闪回，已有流水 DOM 不重建，出牌与组牌动画不重播；再切换顺/逆时针确认上下家姓名只按固定座位环镜像。
- 在牌局中切断网络后等待数秒再恢复，确认离线期间牌桌不清空、按钮不可操作，联网后无需重新输入昵称或选择房间，并接受托管期间已经推进的服务端最新牌局。
- 保持牌局和浏览器页面均打开，只重启服务容器；确认页面先显示自动恢复，再回到完全相同的 roomId、座位、手牌和待响应牌。重连宽限内不得先替真人操作，恢复后可继续当前决定。
- 对局中直接刷新页面，确认无需点击即可回到原座；再用失效 roomId 验证页面明确停止重试并能返回首页。
- 保持旧页面不关闭，再开一个同源新窗口恢复同一座位；确认旧页显示座位已在其他窗口恢复且保持冻结，新页两秒以上不被旧页抢回。
- 好友房由两名真人和机器人混合完成一局，验证房主转移与个人退出不会让整桌断开。
- 用两台设备选择“快速配桌”，确认进入同桌、倒计时不会因刷新重置；其中一人结算后重新配桌，另一人应继续留在原结算页。
- 好友房选择“本桌累计”后连续完成两局，中途分别使用“下一局”和“全桌返回大厅”，核对四席累计值没有清零或重复计算；最后解散房间，确认返回模式选择。

## 5. 部署后冒烟测试

按 [DEPLOYMENT.md](DEPLOYMENT.md) 更新 iMac 后：

1. 确认容器均为运行状态，服务健康检查返回成功。
2. 使用 iMac 覆盖文件部署后访问 `http://imac.tajuren.cn/`，确认首页资源、同源 API、WebSocket、单人练习和快速配桌可用；`:3000` 旧书签仍可打开同一版本，宿主机 2567 不再监听。这里的无端口地址是 HTTP 80 测试入口，不是 HTTPS；正式环境另行验证 HTTPS/WSS。
3. 并发请求两次 `/room-id`，确认返回不同 roomId；同时开启两局练习，确认两边各自只有一名真人和三名机器人。
4. 用两个独立浏览器点击快速配桌，确认汇入同桌；点击立即补位后进入一局，检查牌堆造型、对手余牌数、操作坞和特殊牌提示。
5. 创建一个房间但不建立 WebSocket；在宽限期内连续重启两次服务，确认倒计时不会因重启延长，并在原等待房空闲截止时间后确认该 roomId 已不能加入。
6. 用专用测试档案凭证完成一次 PUT/GET，确认响应禁止缓存且 Redis 中存在对应摘要键；不得在命令输出或日志中暴露真实玩家凭证。
7. 在活动牌局中记录 roomId、本人座位、手牌张数与待响应牌，只强制重建 server 容器且保持 Web 容器 ID 不变；页面应经由同源网关自动恢复到同一决定。随后完成一次出牌，确认不是只恢复了静态画面。
8. 重建服务容器后再次读取测试档案，确认命名卷中的统计仍在；检查日志只显示恢复房间数量，不包含 token 或私有手牌。
9. 正常退出或解散一桌后再重启服务，确认该房间没有从旧快照复活。
10. 对比部署 commit 与本地已验证 commit，避免只重建了旧代码。

同源网关的首轮检查可直接运行：

```bash
npm run smoke:imac-gateway
```

该脚本会在远端容器内执行 `nginx -t`，再从浏览器侧验证无端口页面、同源 `/health`、建房 HTTP 请求、`/matchmake/*` WebSocket、`:3000` 兼容入口以及宿主机 2567 已关闭。它只创建并正常退出一局练习，不重启服务，也不输出房号、身份凭据或手牌。

部署级的活动房恢复可使用受保护的 smoke 脚本。它会真实创建一局并重启目标服务，必须在确认当前环境允许短暂断连后显式解锁；脚本只输出座位、手牌数量和恢复结论，不输出房间 token 或牌面明细：

```bash
LIVE_RECOVERY_SMOKE=1 npm run smoke:live-recovery
```

普通模式执行 `docker compose restart server`。网关发布前必须再执行一次真正替换后端容器的严格模式，并由脚本断言 Web 容器没有随之重启：

```bash
LIVE_RECOVERY_SMOKE=1 LIVE_RECOVERY_RECREATE_SERVER=1 LIVE_RECOVERY_PHASE=playing \
  npm run smoke:live-recovery
```

`LIVE_RECOVERY_PHASE` 默认为 `declaring`；设为 `playing` 后，脚本会先推进到真人等待确认出牌，重建后逐张核对手牌，再实际打出一张牌。默认目标为 `imac`、`~/workspace/lin594/sise` 和同源 `http://imac.tajuren.cn/`；页面、HTTP API 与 WebSocket 都经过 Web 网关。其他部署可通过 `LIVE_RECOVERY_SSH_HOST`、`LIVE_RECOVERY_REMOTE_PATH`、`LIVE_RECOVERY_BASE_URL`、`LIVE_RECOVERY_BACKEND_URL` 覆盖；路径仅接受不含空格和 shell 控制符的安全形式。

可直接从本地测试仓库复用部署后浏览器回归（测试机地址按实际环境替换）：

```bash
PLAYWRIGHT_BASE_URL=http://imac.tajuren.cn PLAYWRIGHT_USE_EXTERNAL_SERVERS=1 \
  npx playwright test tests/e2e/next-round-confirm.spec.ts
PLAYWRIGHT_BASE_URL=http://imac.tajuren.cn PLAYWRIGHT_USE_EXTERNAL_SERVERS=1 \
  npx playwright test tests/e2e/mobile-responsive.spec.ts --grep "keeps eight readable hand cards"
PLAYWRIGHT_BASE_URL=http://imac.tajuren.cn PLAYWRIGHT_USE_EXTERNAL_SERVERS=1 \
  npx playwright test tests/e2e/quick-matchmaking.spec.ts --grep "groups humans|leaving quick-match"
```

下一局用例在 localhost 使用受限调试场景以缩短回归时间；连接外部部署时不要求、也不允许生产调试桥，而是让两名真人座位从界面开启托管，由真实牌局自然完成后再验证结算确认和下一局同步。

## 6. 文档链接检查

文档改名或归档后，使用 `rg` 搜索旧文件名，并逐一确认 Markdown 的相对链接存在。档案可以保留历史文字，但当前 `README.md`、`docs/README.md` 与六份权威文档不得链接到已移走的旧路径。

## 7. iOS Edge 与微信邀请真机验收

浏览器自动化只能验证分享参数和网页行为，不能替代以下真机检查：

1. 在 iOS Edge 打开正式 HTTPS 站点并创建好友房，点击“复制邀请链接”，在微信粘贴；应只有完整 URL，没有说明文字或附件。
2. 点击“邀请牌友”，使用系统分享面板的复制操作后在微信粘贴；应得到包含完整 URL 的邀请文字，不出现无法打开的链接附件。
3. 在另一台设备打开粘贴的 URL，确认进入对应好友房的昵称页，提交后能选择空座位。
4. 记录 iOS、Edge 和微信版本与两条路径的结果；未完成真机检查时不得宣称微信兼容已验证。


## 中央抓牌回归

`central-draw.spec.ts` 覆盖牌背飞入、停留、翻面、真实吃牌组合、普通响应碰，以及金条直接胡／单吃自动胡的结算。服务端 `central-draw.test.ts` 检查一次轮询、C 独占本地机会、流水归属、真实移牌 ID、单吃无重复计分和揭晓前计时冻结。同步后同时回归邀请链接复制、移动端布局和 stateRevision 去重。
